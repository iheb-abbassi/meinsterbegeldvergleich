# Version 1 - Client Version

This is the old one-file widget version.

- Paste `index.html` directly into the IONOS HTML widget.
- EmailJS runs directly in the browser.
- Images are stored in `images/` and referenced through GitHub raw URLs.
- No Vercel backend is required for this version.

